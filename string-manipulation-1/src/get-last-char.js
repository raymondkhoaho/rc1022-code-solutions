/* exported getLastChar */
/* Result = retrieve last character of a string.
input: any js string. output: return last character of string.
function: access string at last index. last index = length of string - 1. */

function getLastChar(string) {
  return string[string.length - 1];
}
