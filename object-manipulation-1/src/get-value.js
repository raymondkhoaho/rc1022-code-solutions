/* exported getValue */
/* Result = value of object.key
input: 2 parameters, object and string
function that returns value at key in object. */

function getValue(object, key) {
  return object[key];
}
