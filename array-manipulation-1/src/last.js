/* exported last */
/* result = return last lement of array (last index)
create variable for last index (length of array minus 1);
return last index of array. */

function last(array) {
  var lastIndex = array.length - 1;
  return array[lastIndex];
}
